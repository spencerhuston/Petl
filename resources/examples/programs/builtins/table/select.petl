let t1 = createTable(
    ${
        name: string,
        age: int,
        salary: string
    },
    [("Alice", 27, "$60000"),
     ("Bob", 45, "$100000"),
     ("Mark", 23, "$45000")]
);

println(t1 |> select(["name", "age"], "age in 25~45") |> collect())
#println(collect(select(t1, ["name", "age"], "age in 25~45")))
