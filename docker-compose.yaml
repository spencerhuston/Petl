services:
  fastapi:
    build:
      context: .
      dockerfile: resources/fastapi/FastApiDockerfile
    container_name: fastapi
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    depends_on:
      - ollama
      - redis
    restart: unless-stopped

  redis:
    image: "redis:latest"
    container_name: redis
    hostname: redis
    expose:
      - "6379"
    command: ["redis-server"]
    restart: unless-stopped

  ollama:
    build:
      context: .
      dockerfile: resources/ollama/OllamaDockerfile
    container_name: ollama
    hostname: ollama
    expose:
      - "11434"
    restart: unless-stopped