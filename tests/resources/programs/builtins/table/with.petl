let t = createTable(
    ${
        name: string,
        age: int,
        salary: string
    },
    [("Alice", 27),
    ("Bob", 45),
    ("Mark", 23)]
);
println(collect(t |> with("salary", ["$60000", "$100000", "$45000"])))