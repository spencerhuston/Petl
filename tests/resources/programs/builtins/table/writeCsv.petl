let t = createTable(
    ${
        name: string,
        age: int,
        salary: string
    },
    [("Alice", 27, "$60000"),
    ("Bob", 45, "$100000"),
    ("Mark", 23, "$45000")]
);
println(if writeCsv("resources/csvs/test_write", t, true) "Created successfully" else "Failed")